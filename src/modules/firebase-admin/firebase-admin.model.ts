export interface IFirebaseExchange {
  symbols: string[];
}

export enum FirebaseExchangeKeys {
  BINANCE_SPOT = 'binance-spot',
  BINANCE_FEATURES = 'binance-features',
}

export enum FirebaseErrors {
  NO_DATA = 'No data',
}
